# AI 应用从 Demo 到生产的工程检查清单

> **这是"标准"，不是"教程"。**
>
> 教程教你"怎么做"，这份清单让你"自查做没做、做没做对"。
>
> 用法：你已经做了一个 AI 应用（或做到一半），拿这份清单逐项对照，检查自己漏了什么、哪里不达标。
> 每一项都标注了 [博思AI智能体](https://github.com/ysy0915/chat-system) 的真实做法作为参考范本——它不是空谈的"正确废话"，而是**有人真的这么做过、且扛住了生产**。

---

## 〇、使用说明

- **勾选标准**：✅ 已达标 / ⚠️ 部分达标 / ❌ 未做
- **红线项**（🔴）：未做 = 不允许上生产，必须先补
- **评分**：每关按子项计分，总分 100 分（见文末评分表）

---

## 一、架构与拆分（8 项）

### 1.1 服务拆分依据
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 是否按"变更频率"拆分 | 变更最频繁的模块能独立发布，不拖累其他模块 | AI 编排（chat-core）变更最频繁，独立成服务 |
| 是否按"资源边界"拆分 | 需要独立扩容的模块单独成服务 | LLM 接入要独立扩容，拆成 chat-llm |
| 是否按"故障域"隔离 | 一个模块挂了不拖垮全站 | 游戏/多模态/LLM 按业务域隔离 |

### 1.2 扩展性设计
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 新增能力是否"填充"而非"重构" | 加新厂商/新工具不改核心代码 | 模型 SPI 策略工厂，新增厂商只加实现类 |
| 配置是否可热更新 | 改配置不重启 | DB 为真相源 + 60s 定时刷新，改 key 无需重启 |

### 1.3 高可用
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 核心服务是否有冗余 | 任一实例挂了服务不中断 | core 双实例（9090/9092），web 轮询 |
| 跨节点状态是否一致 | 会话/消息不因实例切换丢失 | 跨节点广播 + Redis 会话 + nodeId 固定 |
| 是否有动态扩缩容能力 | 压力大了能加实例 | Nacos 服务发现 + 动态 upstream + scale 脚本 |

---

## 二、可靠性（10 项）

### 2.1 调用保护
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 是否有熔断 | 上游挂了快速失败，不拖垮下游 | Resilience4j 熔断 |
| 🔴 是否有超时 | 不无限等待 | TimeLimiter 超时 |
| 是否有重试 | 幂等操作才重试，有退避 | 死信队列 + 指数退避 |
| 是否有限流 | 防上游打爆/防自身被刷 | 限流（IP/用户/敏感接口三级） |

### 2.2 降级与兜底
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 主链路失败是否有降级路径 | 主模型挂了能切备用 | 多模型路由 + 故障转移 |
| 全链路失败是否有兜底响应 | 用户有反馈而非白屏 | 全局异常处理 + 统一错误响应 |

### 2.3 消息与任务可靠性
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 消息是否持久化 | 重启不丢消息 | RabbitMQ 持久化 + manual ack |
| 🔴 是否有死信/重投机制 | 失败消息不静默丢失 | DLX 死信 + 指数退避重投 |
| 是否有对账/兜底扫描 | 卡住的任务能自动恢复 | Reconciler 30s 对账（ZSet 索引） |
| 并发控制是否原子 | 双实例下不超发/不泄漏 | Redis Lua 原子限流 |

---

## 三、性能（6 项）

| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 首 token 延迟是否可控 | 有指标，且做过优化 | 豆包换 mini：2.2s → 0.6~1.0s |
| 缓存是否真的命中 | 读写 key 一致，不失效 | 修掉缓存读写 key 不一致 bug |
| 是否做过压测 | 知道 P50/P99，非"感觉不慢" | 500 并发，P50 154ms |
| 慢请求能否定位根因 | 从"慢"追到"为什么慢" | 11s→1-3s：tool_call_id + 豆包 reasoning |
| 是否避免无界增长 | 无内存/队列无界增长 OOM 风险 | 有界队列 + 30min TTL 清理 |
| 连接池是否合理 | 不因连接耗尽卡死 | HikariCP 池大小按需调优 |

---

## 四、安全（12 项）

### 4.1 密钥管理（最容易被忽略、后果最严重）
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 密钥能进仓库吗 | 提交前被拦截，非提交后补救 | pre-commit 钩子 + CI gitleaks 双保险 |
| 🔴 密钥是否与代码分离 | 存 DB/环境变量，代码无明文 | DB llm_provider_props 为真相源 |
| 密钥是否可轮换 | 改 key 不重启 | 60s 定时刷新，改 key 无需重启 |

### 4.2 鉴权与访问控制
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 读/写/订阅三级是否都鉴权 | 不只拦读，写和订阅也要拦 | 方法级鉴权 + WebSocket 订阅级鉴权 |
| 管理面是否独立鉴权 | 管理接口不能普通用户碰 | X-Admin-Password 独立鉴权 |
| 弱密钥是否有防护 | 弱密钥拒绝启动 | JWT 密钥 <32 字节拒绝启动 |

### 4.3 输入与注入防护
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 上传是否有类型白名单 | 防任意文件上传 | 扩展名白名单 + 10MB 兜底 |
| 🔴 SSRF 是否防护 | 防内网地址访问 | SSRF 防护 + DNS 重绑定防护 |
| SQL 注入是否防了 | 参数化查询，危险词拦截 | SQL 白名单 + 危险词正则 |

### 4.4 内容安全
| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 是否有内容安全检测 | 输入+输出都检 | 本地词库 + 阿里云，输入+输出双检 |
| 🔴 拦截失败是否 fail-close | 检测挂了不放行 | fail-close，异常拒绝放行 |
| 日志是否脱敏 | 不泄露 JWT/签名 URL/密钥 | 日志脱敏全覆盖 |

---

## 五、可观测性（6 项）

| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 🔴 系统指标是否有告警 | 内存/CPU/磁盘异常主动发现 | Prometheus + Alertmanager + 钉钉 |
| 业务指标是否有监控 | 命中率/降级率/成本可见 | 业务指标 AOP 切面采集 |
| 告警是否有分级 | 不是所有告警都一样紧急 | 系统 8 + 业务 4 条规则分级 |
| 是否有链路追踪 | 一次请求全链路可追 | traceId 全链路透传 |
| 排障能否快速定位 | 告警到根因几分钟 | 结构化日志 + 分服务日志 |
| 监控是否有版本兼容 | 不因版本升级失效 | micrometer 锁 1.11.6（版本坑已记录） |

---

## 六、测试与质量（6 项）

| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 单测是否真断言 | 断言真实行为，非为覆盖率 | 933 用例全绿 |
| 弱测试是否清零 | 无反射/仅构造/空断言 | 弱测试专项清零 |
| 是否有端到端/集成测试 | 真实链路走一遍 | 关键链路集成测试 |
| 是否有关键链路测试 | 缓存/路由/熔断有专项 | CacheManager/Registry/CircuitBreaker 专项 |
| 是否有代码质量门禁 | 违规能自动拦截 | Checkstyle 0 违规 + PMD 收敛 |
| 是否压测可复现 | 一键跑压测，结果可对比 | 压测脚本一键化 + 落盘对比 |

---

## 七、文档与留痕（6 项）

| 检查点 | 判定标准 | 范本：博思AI 怎么做 |
|--------|---------|---------------------|
| 架构决策是否记录 | 每个关键决策有 ADR | 25+ 条 ADR |
| 部署手册别人能照着做吗 | 不依赖"我脑子里知道" | 部署手册 + 一键脚本 |
| 排障手册是否可执行 | 症状→根因→修复，可照做 | 故障排查手册 |
| 是否有开发时间线 | 过程可追溯，非只有结果 | 研发时间线（服务器日志还原） |
| 是否有中英文文档 | 外国人也能入门 | README 中英双语 |
| 是否有架构图 | 一张图看懂系统 | 架构图 + ER 图 |

---

## 八、评分表（总分 100）

| 关卡 | 子项数 | 权重 |
|------|--------|------|
| 一、架构与拆分 | 8 | 15 分 |
| 二、可靠性 | 10 | 20 分 |
| 三、性能 | 6 | 15 分 |
| 四、安全 | 12 | 20 分 |
| 五、可观测性 | 6 | 10 分 |
| 六、测试与质量 | 6 | 10 分 |
| 七、文档与留痕 | 6 | 10 分 |
| **总计** | **54 项** | **100 分** |

> 计分方式：每关得分 = 该关权重 ×（✅项数 + ⚠️项数×0.5）/ 该关子项数。红线项（🔴）未做，该关直接记 0 分。

### 结论分级
- **≥ 85 分**：生产级成熟，可放心上线
- **70-84 分**：生产级达标，可上线（红线项必须全 ✅）
- **55-69 分**：接近生产级，补齐红线项后上线
- **< 55 分**：demo 阶段，先补架构与可靠性

---

## 附：为什么是这 54 项

这份清单来自一个真实项目的 19 天演进（从 demo 到生产），**每一项都对应一个"不做到就会出事"的真实教训**：

- **架构**：不按变更频率拆，改一个功能要重启全站
- **可靠性**：不默认"会出错"，一个上游抖动就能拖垮全站
- **性能**：不定位根因，"慢"永远只是"感觉"
- **安全**：密钥进了仓库，删都删不干净；拦截 fail-open，等于没拦
- **可观测**：没有告警，故障只能靠用户来报
- **测试**：为覆盖率写的测试，一个都挡不住回归
- **文档**：没有留痕，三个月后你自己都看不懂

**这 54 项，是"AI 应用从 demo 到生产"的完整关卡。少一关，都不是"做全了"。**
