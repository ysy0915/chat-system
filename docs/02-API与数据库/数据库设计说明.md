# 数据库设计说明

> MySQL 表结构、Redis 键设计、索引策略与数据字典

> 🔗 表间关系总览（ER 图，Mermaid 渲染）见 [数据库ER图.md](数据库ER图.md)

---

## 一、MySQL 表结构

### 1.1 核心业务表

#### `users` — 用户表

```sql
CREATE TABLE users (
  id           BIGINT AUTO_INCREMENT PRIMARY KEY,
  email        VARCHAR(255) NOT NULL UNIQUE,
  password_hash VARCHAR(255) NOT NULL,
  name         VARCHAR(100),
  nickname     VARCHAR(100),
  guest_name   VARCHAR(100),
  role         VARCHAR(32) DEFAULT 'user',
  created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at   TIMESTAMP NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 说明 |
|------|------|
| role | `user` / `admin` |
| nickname | 用户昵称（注册/设置可改） |
| guest_name | 游客名（未注册游客对话使用） |
| deleted_at | 软删除标记 |

---

#### `messages` — 消息表

```sql
CREATE TABLE messages (
  id        BIGINT AUTO_INCREMENT PRIMARY KEY,
  req_id    CHAR(36) NOT NULL,
  user_id   BIGINT NOT NULL,
  question  TEXT NOT NULL,
  summary   VARCHAR(200) NULL COMMENT '对话摘要（AI 自动生成，见 docs/sql/add_summary_field.sql）',
  answer    JSON NULL,
  status    VARCHAR(32) NOT NULL,
  provider  VARCHAR(64) NULL,
  model     VARCHAR(128) NULL,
  metadata  JSON NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY uq_messages_reqid (req_id),
  INDEX idx_user_created (user_id, created_at),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 说明 |
|------|------|
| `req_id` | UUID v4，幂等键，防止重复处理 |
| `status` | `queued` / `processing` / `done` / `failed` |
| `answer` | JSON 格式，包含各模型回复 |
| `metadata` | 扩展字段（客户端语言、trace_id 等） |

---

#### ~~`model_configs`~~ — 模型配置表（[B档] 已删除）

> 🗑 2026-08-13 迁移后退役、同日从生产库 DROP（备份 `/opt/app/backup/deprecated_tables_20260813.sql`）。运行时统一读取 `llm_provider_config` / `llm_provider_props` / `llm_model_config` / `llm_model_props` 四张新表（见 `llm_routing_schema.sql`）。数据迁移脚本 `migrate_model_configs_to_llm.sql` 保留作历史参考，模型 id 保持一致。下方保留原表结构仅作历史追溯。

```sql
CREATE TABLE model_configs (
  id                BIGINT AUTO_INCREMENT PRIMARY KEY,
  provider          VARCHAR(64) NOT NULL,
  model             VARCHAR(128) NOT NULL,
  api_key_encrypted TEXT NOT NULL,
  meta              JSON NULL,
  priority          INT DEFAULT 100,
  enabled           BOOLEAN DEFAULT TRUE,
  created_at        TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at        TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 说明 |
|------|------|
| `provider` | 模型提供商 (qwen / deepseek / doubao) |
| `api_key_encrypted` | 加密存储的 API Key（迁移后存于 `llm_provider_props`） |
| `priority` | 路由优先级，数值越小优先级越高（迁移后存于 `llm_model_config.priority`） |
| `enabled` | 是否启用，可运行时切换（迁移后存于 `llm_model_config.enabled`） |
| `meta` | 额外参数 (baseUrl, temperature, maxTokens 等)（baseUrl 迁移后存于 `llm_model_props` / `llm_provider_config.base_url`） |

新表映射关系：

| ModelConfig 视图 | 新表来源 |
|------|------|
| `id` | `llm_model_config.id`（与旧 id 一致） |
| `provider` | `llm_provider_config.provider_name` |
| `model` | `llm_model_config.model_name` |
| `apiKeyEncrypted` | `llm_provider_props`(prop_key='api_key').prop_value |
| `metaJson.baseUrl` | `llm_model_props`(prop_key='base_url') → 回退 `llm_provider_config.base_url` |
| `modelType` | `llm_model_config.model_type`（沿用旧枚举） |

---

### 1.2 功能模块表

#### `rag_knowledge_bases` — 知识库表

```sql
CREATE TABLE rag_knowledge_bases (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '知识库ID',
    name            VARCHAR(100) NOT NULL COMMENT '知识库名称',
    description     VARCHAR(500) DEFAULT '' COMMENT '描述',
    document_count  INT DEFAULT 0 COMMENT '文档数量',
    total_chunks    BIGINT DEFAULT 0 COMMENT '总分片数',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 说明 |
|------|------|
| `document_count` | 冗余计数，方便列表展示 |
| `total_chunks` | 总向量分片数（Milvus 索引量） |

#### `rag_documents` — 知识库文档表

```sql
CREATE TABLE rag_documents (
    id                BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '文档ID',
    knowledge_base_id BIGINT NOT NULL COMMENT '所属知识库ID',
    file_name         VARCHAR(255) NOT NULL COMMENT '原始文件名',
    source            VARCHAR(512) DEFAULT '' COMMENT '来源标记',
    chunk_count       INT DEFAULT 0 COMMENT '分片数量',
    file_size         BIGINT DEFAULT 0 COMMENT '文件大小(字节)',
    status            VARCHAR(20) DEFAULT 'pending' COMMENT 'pending/processing/done/error',
    error_message     TEXT COMMENT '失败原因',
    created_at        DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_kb_id (knowledge_base_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 说明 |
|------|------|
| `status` | `pending` → `processing` → `done` / `error` |
| `chunk_count` | 切分后写入 Milvus 的向量条数 |

#### `debate_records` — 辩论记录表

存储观点辩论场的对话记录，包含辩题、各方观点、用户追问等。

```sql
CREATE TABLE debate_records (
  id           BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id      BIGINT NOT NULL COMMENT '用户ID',
  user_name    VARCHAR(100) DEFAULT '' COMMENT '用户名',
  question     TEXT NOT NULL COMMENT '辩题',
  final_answer MEDIUMTEXT COMMENT '最终裁决/汇总（JSON 或文本）',
  status       VARCHAR(20) DEFAULT 'done' COMMENT '状态: processing / done / error',
  created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_user_id (user_id),
  INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### `tree_hole_messages` — 情绪树洞表

存储用户的情绪倾诉内容、AI 回复、心情标签等（与 `messages` 完全隔离，DDL 见 `docs/sql/tree_hole_schema.sql`）。

```sql
CREATE TABLE IF NOT EXISTS `tree_hole_messages` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `req_id`      VARCHAR(64)  NOT NULL UNIQUE COMMENT '请求唯一ID',
    `user_id`     BIGINT       NOT NULL COMMENT '用户ID',
    `question`    TEXT         NOT NULL COMMENT '用户输入内容',
    `answer_json` MEDIUMTEXT   DEFAULT NULL COMMENT 'AI回答内容',
    `status`      VARCHAR(20)  NOT NULL DEFAULT 'pending' COMMENT 'pending/done/error',
    `mood`        VARCHAR(50)  DEFAULT NULL COMMENT '情绪标签',
    `created_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`  DATETIME     DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_user_created` (`user_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='情绪树洞对话记录';
```

#### `media_gen_records` — 多模态生成记录表

存储文生图、文生视频、图生 3D 的生成任务记录和结果 URL（DDL 见 `docs/sql/media_gen_records.sql`）。

```sql
CREATE TABLE IF NOT EXISTS media_gen_records (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    prompt VARCHAR(2000) NOT NULL COMMENT '用户输入的提示词',
    media_type VARCHAR(20) NOT NULL COMMENT '类型: image / video / 3d',
    model VARCHAR(100) COMMENT '使用的模型名称',
    media_url VARCHAR(2048) COMMENT 'OSS 上的主文件 URL',
    glb_url VARCHAR(2048) COMMENT '3D GLB 文件 URL（仅3D）',
    obj_url VARCHAR(2048) COMMENT '3D OBJ 文件 URL（仅3D）',
    preview_url VARCHAR(2048) COMMENT '预览图 URL（3D模型预览图）',
    status VARCHAR(20) DEFAULT 'done' COMMENT '状态: done / error',
    error_msg VARCHAR(500) COMMENT '错误信息（失败时）',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='媒体生成记录';
```

#### `online_count_records` — 在线人数历史快照

监控页数据源，按时间点记录各页面在线人数。

```sql
CREATE TABLE online_count_records (
  id          BIGINT AUTO_INCREMENT PRIMARY KEY,
  page        VARCHAR(64) NOT NULL COMMENT '页面标识',
  count       INT NOT NULL COMMENT '在线人数',
  recorded_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '记录时间',
  INDEX idx_page_time (page, recorded_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### `attachments` — 附件表

```sql
CREATE TABLE attachments (
  id         BIGINT AUTO_INCREMENT PRIMARY KEY,
  message_id BIGINT NULL,
  uploaded_by BIGINT NOT NULL,
  storage_url VARCHAR(1024) NOT NULL,
  mime_type  VARCHAR(128),
  filename   VARCHAR(255),
  size       BIGINT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### 1.3 LLM 路由配置表（2026-08-13 三源归一后唯一运行时数据源）

> 完整 DDL + 示例数据见 `docs/sql/llm_routing_schema.sql`。旧 `model_configs` 表已于 2026-08-13 迁移后 DROP（备份 `/opt/app/backup/deprecated_tables_20260813.sql`）。

#### `llm_provider_config` — 大模型提供商表

```sql
CREATE TABLE IF NOT EXISTS llm_provider_config (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键',
    provider_name   VARCHAR(100) NOT NULL COMMENT '提供商名称 deepseek/qwen/doubao/openai',
    base_url        VARCHAR(500) NOT NULL COMMENT 'API 基础地址',
    auth_type       VARCHAR(50)  NOT NULL DEFAULT 'api_key' COMMENT '认证方式: api_key / oauth2 / iam',
    invoke_type     VARCHAR(20)  NOT NULL DEFAULT 'rest' COMMENT '调用方式: rest (HTTP REST API) / sdk (OpenAI SDK)',
    enabled         TINYINT(1)   NOT NULL DEFAULT 1 COMMENT '是否启用',
    is_default      TINYINT(1)   NOT NULL DEFAULT 0 COMMENT '是否默认提供商',
    priority        INT          NOT NULL DEFAULT 0 COMMENT '优先级(越小越优先)',
    description     VARCHAR(500) DEFAULT '' COMMENT '描述',
    created_at      DATETIME     DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_provider_name (provider_name),
    INDEX idx_enabled_priority (enabled, priority)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='大模型提供商配置表';
```

| 字段 | 说明 |
|------|------|
| `provider_name` | 提供商唯一名（deepseek / qwen / doubao / openai） |
| `invoke_type` | `rest`（HTTP REST API）/ `sdk`（OpenAI SDK），v2 新增 |
| `api_key` | 不在此表，存 `llm_provider_props`（prop_key=`api_key`，SECRET 类型） |

#### `llm_provider_props` — 提供商 KV 扩展表

```sql
CREATE TABLE IF NOT EXISTS llm_provider_props (
    id                 BIGINT AUTO_INCREMENT PRIMARY KEY,
    provider_config_id BIGINT       NOT NULL COMMENT '关联 llm_provider_config.id',
    prop_key           VARCHAR(100) NOT NULL,
    prop_value         TEXT         NOT NULL,
    prop_type          VARCHAR(20)  NOT NULL DEFAULT 'STRING' COMMENT 'STRING/INT/BOOL/SECRET',
    description        VARCHAR(300) DEFAULT '',
    INDEX idx_provider_prop (provider_config_id, prop_key),
    CONSTRAINT fk_provider_props FOREIGN KEY (provider_config_id)
        REFERENCES llm_provider_config(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='大模型提供商 KV 扩展表';
```

| 字段 | 说明 |
|------|------|
| `prop_type` | `STRING` / `INT` / `BOOL` / `SECRET`（密钥类在管理后台脱敏展示） |

#### `llm_model_config` — 模型配置表

```sql
CREATE TABLE IF NOT EXISTS llm_model_config (
    id                 BIGINT AUTO_INCREMENT PRIMARY KEY,
    provider_config_id BIGINT       NOT NULL COMMENT '关联 llm_provider_config.id',
    model_name         VARCHAR(100) NOT NULL COMMENT '模型名 deepseek-chat / gpt-4o',
    display_name       VARCHAR(200) DEFAULT '' COMMENT '展示名称',
    model_type         VARCHAR(50)  NOT NULL DEFAULT 'chat' COMMENT 'chat / embedding / rerank / vision',
    max_tokens         INT          DEFAULT 4096 COMMENT '最大 token 数',
    enabled            TINYINT(1)   NOT NULL DEFAULT 1,
    is_default         TINYINT(1)   NOT NULL DEFAULT 0,
    priority           INT          NOT NULL DEFAULT 0 COMMENT '模型级优先级',
    description        VARCHAR(500) DEFAULT '',
    created_at         DATETIME     DEFAULT CURRENT_TIMESTAMP,
    updated_at         DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_provider_model (provider_config_id, model_name),
    INDEX idx_provider_enabled (provider_config_id, enabled),
    CONSTRAINT fk_model_provider FOREIGN KEY (provider_config_id)
        REFERENCES llm_provider_config(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='大模型配置表';
```

| 字段 | 说明 |
|------|------|
| `model_type` | `chat` / `embedding` / `rerank` / `vision` |
| `max_tokens` | 默认 4096（DeepSeek 系 65536） |

#### `llm_model_props` — 模型 KV 扩展表

```sql
CREATE TABLE IF NOT EXISTS llm_model_props (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    model_config_id BIGINT       NOT NULL COMMENT '关联 llm_model_config.id',
    prop_key        VARCHAR(100) NOT NULL,
    prop_value      TEXT         NOT NULL,
    prop_type       VARCHAR(20)  NOT NULL DEFAULT 'STRING',
    description     VARCHAR(300) DEFAULT '',
    INDEX idx_model_prop (model_config_id, prop_key),
    CONSTRAINT fk_model_props FOREIGN KEY (model_config_id)
        REFERENCES llm_model_config(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='大模型 KV 扩展表';
```

> 典型键：`base_url`（模型级覆盖提供商地址）、`temperature` 等；`api_key` 位于提供商级 props。

---

### 1.4 AI 能力扩展表

#### `user_profiles` — 用户画像表（L3 长期记忆）

> Redis 画像为主缓存（`user_profile:{scene}:{userId}`），本表持久化兜底；DDL 见 `docs/sql/user_memory_schema.sql`。

```sql
CREATE TABLE IF NOT EXISTS user_profiles (
  id                BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id           BIGINT       NOT NULL COMMENT '用户ID',
  scene             VARCHAR(32)  NOT NULL DEFAULT 'personal' COMMENT '场景 personal/treehole/chat',
  scene_desc        VARCHAR(500) DEFAULT '' COMMENT '用户当前情景（一句话）',
  emotions_json     JSON         DEFAULT NULL COMMENT '近期情绪列表 JSON数组',
  preferences_json  JSON         DEFAULT NULL COMMENT '用户偏好列表 JSON数组',
  contexts_json     JSON         DEFAULT NULL COMMENT '背景信息列表 JSON数组',
  source_count      INT          DEFAULT 0 COMMENT '累计来源对话数',
  updated_at        TIMESTAMP    DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  created_at        TIMESTAMP    DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  UNIQUE KEY uk_user_scene (user_id, scene)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户画像（L3 长期记忆）';
```

#### `skill_registry` — 技能注册中心表（Step3 技能自进化）

```sql
CREATE TABLE IF NOT EXISTS skill_registry (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  name            VARCHAR(128)  NOT NULL COMMENT '技能名（唯一，英文驼峰）',
  description     VARCHAR(500)  DEFAULT '' COMMENT '技能描述/适用场景',
  language        VARCHAR(32)   DEFAULT 'java' COMMENT '代码语言 java/python',
  code            MEDIUMTEXT    COMMENT '生成的函数代码（可直接执行）',
  trigger_prompt  TEXT          COMMENT '触发指令（何时调用、参数怎么传）',
  source_trace    TEXT          COMMENT '来源追溯（触发请求+工具链摘要）',
  usage_count     INT           DEFAULT 0 COMMENT '被使用次数',
  status          TINYINT       DEFAULT 1 COMMENT '状态 1启用 0禁用',
  created_at      TIMESTAMP     DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at      TIMESTAMP     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  UNIQUE KEY uk_skill_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='技能注册中心（Step3 技能自进化）';
```

#### `tool_registry` — 工具平台化注册表

```sql
CREATE TABLE IF NOT EXISTS tool_registry (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键',
    tool_name   VARCHAR(64)  NOT NULL COMMENT '工具唯一名（与 Tool.getName() 一致）',
    description VARCHAR(1024) DEFAULT '' COMMENT 'AI 可见的工具描述（可被管理面覆盖）',
    parameters  TEXT          DEFAULT NULL COMMENT 'AI 可见的参数 JSON Schema（NULL=用代码默认）',
    enabled     TINYINT(1)    NOT NULL DEFAULT 1 COMMENT '是否启用（0=禁用，LLM 不可见不可调用）',
    scope       VARCHAR(64)   NOT NULL DEFAULT '*' COMMENT '可见范围: * / chat / subagent / 自定义',
    source      VARCHAR(16)   NOT NULL DEFAULT 'DB' COMMENT '声明来源: CODE(代码内置) / DB(管理面声明)',
    created_at  DATETIME      DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_tool_name (tool_name),
    INDEX idx_enabled (enabled)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT ='工具平台化注册表（元数据声明）';
```

> `source=CODE` 的代码内置工具启动时自动注册默认声明；管理面（`/internal/tools`）落库（`source=DB`）可覆盖 `enabled`/`description`/`parameters`/`scope`，无需发版开关工具。未建表时 `ToolRegistry` 容错降级。

#### `rag_chunks` — BM25 关键词分片表（2026-08-15 新增）

混合检索（向量 + 关键词）的关键词侧存储，由 chat-llm `KeywordSearchService` 启动幂等建表（`CREATE TABLE IF NOT EXISTS`），**无需手工迁移**。

```sql
CREATE TABLE IF NOT EXISTS rag_chunks (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    kb_id BIGINT NOT NULL,
    doc_id BIGINT NOT NULL,
    chunk_index INT NOT NULL,
    source VARCHAR(512) DEFAULT '',
    page INT DEFAULT 0,
    text MEDIUMTEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_kb_doc (kb_id, doc_id),
    FULLTEXT KEY ft_rag_text (text) WITH PARSER ngram
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `kb_id` | BIGINT NOT NULL | 知识库 ID |
| `doc_id` | BIGINT NOT NULL | 文档 ID |
| `chunk_index` | INT NOT NULL | 分片序号（文档内从 1 递增） |
| `text` | MEDIUMTEXT NOT NULL | 分片文本（全文索引目标） |
| `page` | INT | 页码（PDF 逐物理页，默认 0） |

- 索引：`idx_kb_doc(kb_id, doc_id)`、`FULLTEXT ft_rag_text(text) WITH PARSER ngram`（中文二元分词，MySQL 5.7.6+/8.0）
- 写入/检索/删除仅在 `HYBRID_KEYWORD_ENABLED=true` 时生效；删除知识库/文档时按 `kb_id`/`doc_id` 联动清理

---

### 1.5 规划/预留表（ER 图已建模，当前代码未落地）

> 下列表出现在《数据库ER图.md》中，但**仓库无 DDL 脚本、无实体类、无 Repository**，属于规划/预留设计。若后续落地，按 ER 图字段建模即可。

| 表 | ER 图字段 | 说明 |
|----|----------|------|
| `user_registrations` | `id`/`user_id`(FK→users.id)/`email`/`username`/`registered_at` | 注册快照记录（ER 图含物理外键 `user_registrations_ibfk_1`） |
| `audit_logs` | `id`/`event_type`/`user_id`/`username`/`ip_address`/`user_agent`/`detail`/`result`/`created_at` | 操作审计日志（登录/敏感操作） |

> 说明：目前登录审计与风控（登录失败锁定 `rate:login-fail:{ip}`、验证码 `captcha:{token}`）走 Redis + Nginx 层，未落 MySQL 审计表。

---

## 二、索引策略

| 表 | 索引 | 类型 | 用途 |
|----|------|------|------|
| `messages` | `uq_messages_reqid` | UNIQUE | 幂等防重 |
| `messages` | `idx_user_created` | 联合索引 | 用户历史查询 |
| `messages` | `idx_status` | 普通索引 | 按状态查询 |
| `messages` | `ft_messages_content` | FULLTEXT | 关键词搜索（`MATCH ... AGAINST`，替代 `LIKE '%kw%'` 全表扫描） |
| `users` | `UNIQUE(email)` | UNIQUE | 登录查询 |
| ~~`model_configs`~~ | `INDEX(provider, model)` | 联合索引 | 🗑 已删除（2026-08-13），模型查找由 `llm_model_config`/`llm_provider_config` 承担 |
| `debate_records` | `idx_user_id` / `idx_created_at` | 普通索引 | 用户历史辩论查询 |
| `treehole_messages` | `idx_user_created(user_id, created_at)` | 联合索引 | 用户树洞历史（分页） |
| `media_gen_records` | `idx_user_id` / `idx_created_at` | 普通索引 | 用户生成历史查询 |
| `online_count_records` | `idx_page_time(page, recorded_at)` | 联合索引 | 按页取在线趋势 |
| `rag_knowledge_bases` | `INDEX(owner_id)` | 普通索引 | 按用户查知识库 |
| `rag_documents` | `idx_kb_id(kb_id)` | 普通索引 | 按知识库查文档 |
| `rag_chunks` | `idx_kb_doc(kb_id, doc_id)` | 联合索引 | 按知识库/文档检索分片 |
| `rag_chunks` | `ft_rag_text(text)` | FULLTEXT ngram | 中文关键词 BM25 检索（`HYBRID_KEYWORD_ENABLED=true` 时生效） |
| `llm_provider_config` | `uk_provider_name(provider_name)` | UNIQUE | 提供商唯一 |
| `llm_provider_config` | `idx_enabled_priority(enabled, priority)` | 联合索引 | 启用+优先级路由排序 |
| `llm_provider_props` | `idx_provider_prop(provider_config_id, prop_key)` | 联合索引 | 提供商 KV 查询 |
| `llm_model_config` | `uk_provider_model(provider_config_id, model_name)` | UNIQUE | 提供商内模型唯一 |
| `llm_model_config` | `idx_provider_enabled(provider_config_id, enabled)` | 联合索引 | 按提供商+启用查模型 |
| `llm_model_props` | `idx_model_prop(model_config_id, prop_key)` | 联合索引 | 模型 KV 查询 |
| `user_profiles` | `uk_user_scene(user_id, scene)` | UNIQUE | 用户+场景唯一画像 |
| `skill_registry` | `uk_skill_name(name)` | UNIQUE | 技能名唯一 |
| `tool_registry` | `uk_tool_name(tool_name)` | UNIQUE | 工具唯一名 |
| `tool_registry` | `idx_enabled(enabled)` | 普通索引 | 按启用过滤 |

> 迁移脚本位置：`docs/db-migrations/V1.1.0__add_messages_index.sql`
> 全文索引迁移：`docs/sql/fulltext_search_indexes.sql`（2026-08 新增，解决消息搜索性能问题）
> BM25 关键词表 `rag_chunks` 由 chat-llm `KeywordSearchService` 启动幂等建表，**无需手工迁移**（结构见上文 1.4）

---

## 三、Redis 键设计

### 3.1 会话跟踪

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| ZSet | `session:{page}` | 页面在线用户集合，score=时间戳 | — |
| String | `online:count:{page}` | 页面实时在线人数 | — |

### 3.2 结果缓存

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| Hash | `model:cache:{reqId}` | AI 回复结果缓存 | 24h |

### 3.3 用户数据

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| List | `user:{uid}:history` | 用户消息 ID 列表 (LPUSH, LTRIM) | — |

### 3.4 并发控制

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| String | `inflight:{req_id}` | 消费锁 (SETNX)，防止重复消费 | 60-300s |

### 3.5 限流

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| Counter | `rate:{uid}` | 用户请求速率计数器 | — |
| Counter | `ip:rate:global:{ip}` | 全站接口限流（IpRateLimitInterceptor，600 次/分钟） | 60s |
| Counter | `ip:rate:sensitive:{ip}` | 敏感接口限流（auth/login、auth/register，10 次/分钟，超限 429"操作过于频繁"） | 60s |
| String | `rate:login-fail:{ip}` | 登录失败锁定（连续 5 次失败锁 15 分钟，第 6 次起 429，统一文案"用户名或密码错误"） | 15min |
| String | `ip:blacklist:{ip}` | 全站 60s 内超 1000 次请求自动拉黑 | 10min |

### 3.6 Multi-Agent 工作流状态（2026-08 新增）

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| String | `agent:plan:{planId}` | 拆解计划（`SubAgentPlan` JSON，供任意实例收敛） | 30min |
| String | `agent:plan:{planId}:meta` | 请求元信息（reqId/userId/question/模型参数） | 30min |
| String | `agent:plan:{planId}:total` | 子任务总数（Worker 收敛触发依据） | 30min |
| String | `agent:plan:{planId}:received` | 已回收结果数（INCR 原子计数） | 30min |
| String | `agent:plan:{planId}:lock` | 收敛分布式锁（SETNX：正常触发 2min / Reconciler 对账 5min） | 2/5min |
| String | `agent:plan:{planId}:converged` | 收敛完成标记（Reconciler 去重，防重复触发） | 30min |
| Hash | `agent:subtask:result:{planId}` | 子任务结果聚合（field=taskId，value=`SubAgentResult` JSON，幂等覆盖） | 30min |
| Counter | `agent:workflow:active` | 全局并行工作流计数（Lua INCR/DECR 原子限流，上限 max-concurrent=8，双实例共享） | 300s |

> 说明：工作流状态全部存 Redis（30min TTL），跨实例收敛依赖这些键；`agent:workflow:active` 用 Lua 脚本保证双实例计数原子性，超限请求自动降级普通流程。

### 3.7 用户画像缓存（2026-08 新增）

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| Hash | `user_profile:{scene}:{userId}` | L3 长期记忆画像（emotions/preferences/contexts JSON），与 `user_profiles` 表互为缓存（表兜底） | 7d |

### 3.8 辩论记忆（2026-08-15 新增）

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| String | `debate:memory:{userId}:{topicHash}` | 观点辩论跨会话记忆（JSON：pro/con/neutral 各方最后一条反思立场 + rounds + updatedAt），同话题二次辩论首轮注入 `historySummary`；无 Redis 静默降级 | 7d |

### 3.9 安全加固（2026-08 新增）

| 数据结构 | Key 模式 | 说明 | TTL |
|---------|---------|------|-----|
| String | `captcha:{token}` | 注册验证码（算术题答案，一次性消费），注册需携带 `captcha_token`/`captcha_answer` | 5min |

迁移脚本位于 `docs/db-migrations/` 和 `docs/sql/`：

### db-migrations (版本化迁移)

| 文件 | 说明 |
|------|------|
| `V1.1.0__add_messages_index.sql` | messages 表添加联合索引 |

### sql (功能模块 DDL)

| 文件 | 说明 |
|------|------|
| `model_configs_update.sql` | 模型配置表更新（🗑 旧表已删，历史脚本仅存档） |
| `rag_schema.sql` | RAG 知识库/文档表 |
| `tree_hole_schema.sql` | 情绪树洞模块表 |
| `media_gen_records.sql` | 多模态生成记录表 |
| `add_summary_field.sql` | 消息摘要字段添加 |
| `llm_routing_schema.sql` | LLM 路由配置 4 表（2026-08-13 三源归一，含示例数据） |
| `user_memory_schema.sql` | 用户画像表 `user_profiles` + 技能注册中心 `skill_registry` |
| `tool_registry_schema.sql` | 工具平台化注册表 `tool_registry` |
| `fulltext_search_indexes.sql` | 消息全文索引 `ft_messages_content` |

### 执行方式

```bash
# 执行未应用的迁移
bash scripts/db-migrate.sh

# 查看迁移状态
bash scripts/db-migrate.sh status
```

---

## 五、MQ 消息格式

### RabbitMQ 消息体

Exchange: `chat.exchange`
Routing Key: `chat.request`

```json
{
  "req_id": "uuid-v4",
  "user_id": 123,
  "question": "用户问题文本",
  "attachments": [{"id": 45, "url": "..."}],
  "preferred_model_config_id": 7,
  "timestamp": 1690680000,
  "metadata": {
    "clientLang": "zh",
    "trace_id": "..."
  }
}
```

角色与职责：
- **生产者**：`MessageController.createMessage()` 入库后发布
- **消费者**：`ChatProcessor.process()` 消费处理
- **容错**：RabbitMQ 不可用时直接同步调用 `chatProcessor.process()` 降级
