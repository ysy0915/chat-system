# Architecture Design

## 1. 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                       Nginx (:80)                            │
│  ├── /            → 前端静态资源 (frontend/dist/)              │
│  ├── /api/*       → chat-web (:8081)                        │
│  └── /ws/*        → chat-web (:8081) WebSocket              │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────┼──────────────────────────────────────┐
│    chat-web (:8081)  │  Web 接入层                            │
│  ├── Controller     │  REST API / WebSocket                 │
│  ├── Security       │  JWT 认证 / IP 限流                     │
│  └── SessionTracker │  WebSocket Session (→ Redis)          │
└──────────────┬──────────────────┬─────────────────┬─────────┘
               │                  │                 │
┌──────────────▼──────┐ ┌─────────▼──────┐ ┌────────▼───────┐
│  chat-core (:9090)  │ │ chat-games     │ │ chat-media     │
│  ├── LLM 策略工厂    │ │ (:8083)        │ │ (:8084)        │
│  ├── LLMClient      │ │ 游戏逻辑        │ │ 多模态生成      │
│  ├── ToolDispatcher │ │ WebSocket      │ │ 3D/图片/视频   │
│  ├── ModelRouter    │ │ 对战/排行榜     │ │                │
│  └── DebateProcessor│ └────────────────┘ └────────────────┘
└──────────────┬──────┘
               │
┌──────────────▼─────────────────────────────────────────────┐
│                      基础设施                                 │
│  MySQL (RDS)  │  Redis  │  RabbitMQ  │  Nacos (:8848)      │
└─────────────────────────────────────────────────────────────┘
```

## 2. LLM 调用架构

### 2.1 策略模式

```
                    ┌──────────────────┐
                    │  LLMClient        │  ← Fluent Builder API
                    │  (场景化调用)       │
                    └───────┬──────────┘
                            │ 委托
                    ┌───────▼──────────┐
                    │  LLMInvoker       │  ← 熔断 / 自愈 / 链路追踪
                    └───────┬──────────┘
                            │ 策略选择
                    ┌───────▼──────────┐
                    │ LLMStrategyFactory│
                    └──┬──────────┬────┘
                       │          │
          ┌────────────▼──┐  ┌───▼─────────────┐
          │ OpenAICompat   │  │ DoubaoStrategy   │
          │ (qwen/deepseek)│  │ (豆包专用格式)     │
          └───────────────┘  └─────────────────┘
```

### 2.2 调用层次

| 组件 | 职责 | 使用者 |
|------|------|--------|
| `LLMClient` (NEW) | Fluent Builder、场景配置预设 | 业务层 |
| `LLMInvoker` | 熔断、链路追踪、自愈重试 | LLMClient |
| `LLMStrategyFactory` | 按 provider 名称分发策略 | LLMInvoker |
| `LLMStrategy` | HTTP 调用 + 响应解析 | Factory |

### 2.3 动态配置（Nacos）

`LLMClientConfig` 通过 `@RefreshScope` 支持运行时调整：
- 重试次数 / 退避间隔
- 超时时间
- 各场景的 temperature / max_tokens / top_p
- 修改 Nacos 配置后无需重启，刷新间隔内自动生效

## 3. 无状态化设计

### 3.1 Session 管理

```
┌──────────────────────────────────────────────────────────┐
│  WebSocketSessionTracker                                  │
│                                                          │
│  移除: localSessions (ConcurrentHashMap)  ← 有状态      │
│  新增: ws:session:page (Redis Hash)      ← 无状态      │
│                                                          │
│  Redis Keys:                                             │
│  ├── ws:session:page      Hash   sessionId → page       │
│  ├── ws:page:{page}       Set    页面下 sessionId 集合    │
│  ├── ws:heartbeat:{sid}   String 心跳时间戳              │
│  └── ws:known:pages       Set    已知页面标识             │
└──────────────────────────────────────────────────────────┘
```

### 3.2 多实例就绪探针

`ReadinessHealthIndicator` 暴露 `/actuator/health/readiness`：
- 验证 Redis 读写能力
- 确认 Session 追踪器可正常工作
- Kubernetes / 负载均衡可根据此探针判断实例是否就绪

### 3.3 水平扩展能力

| 组件 | 状态管理 | 多实例安全 |
|------|---------|-----------|
| chat-web | Session → Redis Hash | ✅ |
| chat-core | 无本地缓存 | ✅ |
| chat-games | 游戏状态 → Redis | ✅ |
| chat-media | 无状态任务 | ✅ |

## 4. 关键设计决策

| 决策 | 原因 |
|------|------|
| LLM 策略模式 | 不同 Provider (豆包/千问/DeepSeek) 调用格式不同 |
| Nacos 动态配置 | 运行时调优 temperature/maxTokens，无需重启 |
| Redis Session 追踪 | 支持 chat-web 多实例部署 |
| 熔断器 (CircuitBreaker) | 防止 LLM 故障雪崩 |
| WebSocket + Redis Pub/Sub | 多实例消息广播 |
