# 测试规范

## 一、测试分层策略

```
┌─────────────────────────────────────────┐
│          E2E 测试 (暂无，未来规划)         │
│   Cypress / Playwright → 模拟真实用户操作  │
├─────────────────────────────────────────┤
│        集成测试 (Maven failsafe)          │
│   测试 Controller + Service + DB 联动     │
├─────────────────────────────────────────┤
│         单元测试 (Maven surefire)         │
│   测试 Service/Util 方法级逻辑            │
└─────────────────────────────────────────┘
```

## 二、当前状态

### 2.1 覆盖率

| 模块 | 指令覆盖率 | 分支覆盖率 | 状态 |
|------|:---:|:---:|------|
| chat-common | ~25% | ~15% | ⬆ 已提升 (空壳→真实测试) |
| chat-core | ~8% | ~5% | 需加强 |
| chat-web | ~8% | ~5% | ⬆ 54 个测试（12 个 Controller 全覆盖，含 KnowledgeBaseControllerTest） |
| chat-games | ~3% | ~2% | 需加强 |
| chat-media | ~3% | ~2% | 需加强 |
| chat-llm | 待统计 | 待统计 | 新模块，待补充测试 |

> CI Profile 最低阈值: 5% (暂不阻断)
> 目标: 第一阶段提升到 30%，第二阶段 60%
> 最近更新：2026-08 完成 W1+W2+W4 (空壳清理 + chat-common 覆盖提升 + chat-web Controller 全覆盖)，并删除 35 个空壳/废弃测试文件
> 前端：`frontend/src/hooks` 新增 vitest 测试（useAuthUser / useAutoScroll），`npm run vitest` 通过

### 2.2 已知问题

- ✅ ~~**15+ 个空壳测试**~~ — 已于 W1 全部替换为真实测试 (AAA 模式 + Mockito mock)
- ✅ ~~**35 个空壳/废弃测试文件**~~ — 2026-08 已全部删除（chat-core 旧 LLM 策略层、chat-common 审计类、chat-llm 空壳 DTO 等）
- **测试依赖外部服务**: 部分测试直接连远程 Redis/MySQL，不可重复
- **LLM 调用未 mock**: Agent/Strategy 测试可能产生真实 API 费用
- **无集成测试**: 没有 `@SpringBootTest` 级别的端到端验证

## 三、单元测试规范

### 3.1 命名约定

```java
// 格式: methodName_StateUnderTest_ExpectedBehavior
@Test
void calculateRiskScore_negativeInput_throwsException() { }

@Test
void routeRequest_visionTask_selectsVisionModel() { }

@Test
void parseJson_malformedInput_returnsFallback() { }
```

### 3.2 结构规范 (AAA 模式)

```java
@Test
void shouldReturnCachedResultWhenAvailable() {
    // Arrange - 准备测试数据
    String query = "什么是 AI?";
    when(cacheService.get("query:123")).thenReturn("AI 是人工智能的缩写");

    // Act - 执行被测方法
    String result = chatService.answer("123", query);

    // Assert - 验证结果
    assertEquals("AI 是人工智能的缩写", result);
    verify(cacheService).get("query:123");
    verify(llmClient, never()).call(any());
}
```

### 3.3 禁止写法

```java
// ❌ 空壳测试 - 无意义
@Test
void testSomething() throws Exception {
    Class.forName("com.example.chat.SomeClass");
    assertNotNull("test");
}

// ❌ 不验证行为
@Test
void testChat() {
    chatService.chat("hello");  // 没有 assert
}

// ❌ 依赖外部资源
@Test
void testDatabase() {
    // 直接连远程 MySQL - 不可重复
    var conn = DriverManager.getConnection("jdbc:mysql://rds...");
}

// ✅ 正确：mock 外部依赖
@Test
void testDatabase() {
    when(userRepository.findById(1L)).thenReturn(Optional.of(mockUser));
    var result = userService.getUser(1L);
    assertEquals("testUser", result.getUsername());
}
```

### 3.4 Mock 策略

| 场景 | 策略 |
|------|------|
| Repository/DAO | 全部 mock |
| 外部 HTTP 调用 | 全部 mock (WireMock / Mockito) |
| LLM API 调用 | 全部 mock，避免产生费用 |
| Redis 缓存 | mock (RedisTemplate / StringRedisTemplate) |
| RabbitMQ | mock (RabbitTemplate / SimpMessagingTemplate) |
| WebSocket 管理 | mock (WebSocketSessionTracker) |
| 纯逻辑 Util 类 | 不需要 mock |
| 值对象/DTO | 不需要 mock |

> **Mock 框架**: Mockito 5.14.2 + ByteBuddy 1.15.11 (兼容 JDK 26)
> Mockito 5.x 内置支持 mock final 类/方法，**不再需要** `mockito-inline` 依赖。
> pom.xml 中统一管理 `mockito.version` 和 `byte-buddy.version` 版本号。

## 四、LLM 调用测试

### 4.1 Agent 工具测试

```java
@Test
void calculatorTool_validExpression_returnsResult() {
    CalculatorTool tool = new CalculatorTool();
    
    String result = tool.execute("2 + 3 * 4");
    
    assertEquals("14.0", result.trim());
}

@Test
void calculatorTool_malformedInput_returnsError() {
    CalculatorTool tool = new CalculatorTool();
    
    String result = tool.execute("not math");
    
    assertTrue(result.contains("错误") || result.contains("error"));
}
```

### 4.2 Provider 测试

```java
@Test
void openAIProvider_parseSSE_extractsTokens() {
    OpenAICompatProvider provider = new OpenAICompatProvider(config, mapper);
    String sseData = "data: {\"choices\":[{\"delta\":{\"content\":\"你好\"}}]}\n\n";
    
    // 使用 mock SSE 流验证 token 提取逻辑
    // ...
}
```

## 五、集成测试规范

### 5.1 何时写集成测试

- Controller 层参数校验 + 序列化
- Service + Repository 事务行为
- WebSocket 连接/断连/重连
- Redis 缓存读写一致性

### 5.2 写法示例

```java
@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)
@AutoConfigureMockMvc
class ChatControllerIntegrationTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private CoreClient coreClient;  // mock 下游依赖

    @Test
    void sendMessage_validRequest_returns200() throws Exception {
        when(coreClient.chat(any())).thenReturn(Map.of("reply", "你好"));

        mockMvc.perform(post("/api/v1/chat/send")
                .contentType(MediaType.APPLICATION_JSON)
                .content("""
                    {"question":"你好","modelId":1}
                    """))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.reply").value("你好"));
    }
}
```

### 5.3 Multi-Agent 工作流生产测试套件（2026-08 新增）

**脚本**：`scripts/test-multiagent-suite.sh`（运行于 Milvus 服务器，`bash /opt/app/test-multiagent-suite.sh [--quick]`）

覆盖 Multi-Agent 并行工作流全链路（web→core→RabbitMQ→Worker→收敛→DB）：

| 用例 | 场景 | 断言 |
|------|------|------|
| T01 | 单请求并行链路（经 web 全链路） | queued → 分发 9 子任务 → 收敛 answerLen ≤1000 → DB done（4 项） |
| T02 | 简单问题不拆解 | 不触发拆解 + 普通流式 DB done（2 项） |
| T03 | 并发限流降级（20 并发） | 8 并行 + 12 降级 = 20 全有去向；并行 ≤10；全部收敛（3 项） |
| T04 | manual ack 可靠性 | 子任务执行完成数 == 分发数（零丢失） |
| T05/T06 | 输出压缩 + DB 终态 | 汇总本套件所有 web 落库请求终态 done（2 项） |

- 全量目标：**PASS=12 / FAIL=0**（2026-08-13 实测通过）
- `--quick` 跳过 T03/T04（避免耗时并发场景）
- 前置清理：Redis `agent:workflow:active` 应归零（上一轮中断测试可能遗留，脚本启动前 `redis-cli del agent:workflow:active`）

## 六、测试数据管理

### 6.1 测试数据原则

1. **每个测试独立**: 不依赖其他测试的副作用
2. **使用 Builder 模式**: 创建测试对象
3. **常量 + 边界值**: 定义合理的测试常量

```java
// 测试数据工厂
public class TestDataFactory {
    public static ModelConfig createTestModel() {
        ModelConfig config = new ModelConfig();
        config.setId(1L);
        config.setName("test-model");
        config.setType("chat");
        config.setEnabled(true);
        return config;
    }

    public static ChatRequest createTestRequest() {
        ChatRequest req = new ChatRequest();
        req.setQuestion("Hello");
        req.setModelId(1L);
        req.setTemperature(0.7);
        return req;
    }
}
```

## 七、CI 中的测试

### 7.1 质量门禁

```xml
<!-- ci profile 中的 JaCoCo 阈值 (pom.xml) -->
<limit>
    <counter>INSTRUCTION</counter>
    <value>COVEREDRATIO</value>
    <minimum>0.05</minimum>  <!-- 当前 5%，逐步提升 -->
</limit>
```

**提升路线**:

| 阶段 | 阈值 | 阻断？ |
|------|:---:|:---:|
| 当前 | 5% | 否 |
| Phase 2 | 20% | 否 |
| Phase 3 | 40% | 是 |
| 目标 | 60% | 是 |

### 7.2 测试报告

CI 每次运行后自动上传:
- **JaCoCo 覆盖率 HTML** → 保留 14 天
- **JUnit 测试结果** → GitHub PR 内嵌展示

## 八、路线图

### 8.1 已完成 (W1-W4, 2026-08)

| 任务 | 内容 |
|------|------|
| W1 空壳清理 | 14 个空壳测试全部替换为 AAA 模式真实测试 |
| W2 chat-common | 12 个测试文件重写 + 2 个新测试类 (GlobalExceptionHandler / AdminAuthUtil) |
| W4 chat-web | 12 个 Controller 测试全覆盖（54 个测试全绿），含代理层 KnowledgeBaseControllerTest |
| W4 前端 | hooks vitest 测试 (useAuthUser / useAutoScroll) |
| Mockito 升级 | 升级到 5.14.2 + ByteBuddy 1.15.11 兼容 JDK 26 |
| 废弃清理 (2026-08) | 删除 35 个空壳/废弃测试文件（chat-core 旧 LLM 策略层、chat-common 审计类、chat-llm 空壳 DTO） |

**新增/重写的测试类**:
- `config/JacksonConfigTest`, `SpaRoutingConfigTest`, `ViewConfigTest`, `TraceIdFilterTest`
- `config/ReadinessHealthIndicatorTest`, `GlobalExceptionHandlerTest`, `CrossNodeMessageListenerTest`
- `config/IdleSessionCleanupTaskTest`
- `security/AdminAuthUtilTest`, `JwtAuthenticationFilterTest`, `IpRateLimitInterceptorTest`
- `service/BroadcastServiceTest`, `OnlineCountSchedulerTest`

> 注：原 `service/AuditServiceTest` 等空壳测试已随 `AuditService` 废弃类一并删除（详见废弃类清理清单）。

### 8.2 进行中

| 周次 | 目标 |
|------|------|
| W3 | chat-core LLM 层 mock 测试 |
| W5+ | 持续补充各模块（games / media / llm 覆盖提升） |
