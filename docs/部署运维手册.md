# 部署运维手册

> 覆盖本机开发、服务器部署、Docker 部署三种场景的完整流程

---

## 一、架构概览

### 服务器拓扑

```
用户浏览器
    │
    ▼
主服务器 112.124.106.108 (Nginx + Redis + RabbitMQ)
    │  ├─ 前端静态资源: /opt/app/static/chat/
    │  ├─ Nginx 反代 /api/ /ws/ → 内网 172.23.172.13:8081
    │  └─ Redis + RabbitMQ 本地
    │
    ▼ 内网
Milvus 服务器 121.40.188.98 (所有 Java 服务)
    ├─ chat-core   :9090 (核心AI服务)
    ├─ chat-web    :8081 (Web接入层，单实例)
    ├─ chat-games  :8083 (游戏服务)
    ├─ chat-media  :8084 (多模态服务)
    └─ Nacos :8848 (注册中心，nacos/nacos)
```

### 技术版本

| 组件 | 版本 |
|------|------|
| Java | 17 |
| Spring Boot | 3.1.6 |
| Spring Cloud | 2022.0.4 |
| MySQL | 8.0 (远程 RDS) |
| Redis | 7.x |
| RabbitMQ | 3.x |
| Nacos | 2.2.3 |
| Node.js | 18+ |
| Vite | 5.x |

---

## 二、本机开发部署

### 2.1 前置准备

```bash
# 安装 Redis
brew install redis && brew services start redis

# 安装 RabbitMQ
brew install rabbitmq && brew services start rabbitmq

# Node.js 18+ (通过 nvm 管理)
nvm install 18 && nvm use 18
```

### 2.2 打包全部模块

```bash
cd /Users/apple/IdeaProjects/chat-system-project
mvn clean install -DskipTests
```

### 2.3 启动服务（至少两个终端）

**终端 1 - chat-core（端口 9090）**：
```bash
cd /Users/apple/IdeaProjects/chat-system-project
java -jar chat-core/target/chat-core-0.0.1-SNAPSHOT.jar \
  --spring.profiles.active=local --server.port=9090
```

**终端 2 - chat-web（端口 8080）**：
```bash
cd /Users/apple/IdeaProjects/chat-system-project
java -jar chat-web/target/chat-web-0.0.1-SNAPSHOT.jar \
  --spring.profiles.active=local --server.port=8080 \
  --app.core.base-url=http://127.0.0.1:9090
```

**终端 3 - 前端开发**：
```bash
cd frontend && npm run dev
```

### 2.4 访问地址

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:5173 |
| Web API | http://localhost:8080/api/v1/* |
| Core 内部 API | http://localhost:9090/internal/* |

---

## 三、生产服务器部署

### 3.1 SSH 密钥

| 用途 | 密钥路径 |
|------|---------|
| 主服务器 (112.124.106.108) | `/Users/apple/Downloads/我的密钥.pem` |
| Milvus 服务器 (121.40.188.98) | `/Users/apple/Downloads/Milvus.pem` |

### 3.2 部署流程

```bash
# 1. 构建前端
cd /Users/apple/IdeaProjects/chat-system-project/frontend
npm run build

# 2. 打包全部后端模块
cd /Users/apple/IdeaProjects/chat-system-project
mvn clean install -DskipTests

# 3. 上传 jar 到 Milvus 服务器
scp -i "/Users/apple/Downloads/Milvus.pem" \
  chat-core/target/chat-core-0.0.1-SNAPSHOT.jar \
  root@121.40.188.98:/opt/app/core/

scp -i "/Users/apple/Downloads/Milvus.pem" \
  chat-web/target/chat-web-0.0.1-SNAPSHOT.jar \
  root@121.40.188.98:/opt/app/web/

scp -i "/Users/apple/Downloads/Milvus.pem" \
  chat-games/target/chat-games-0.0.1-SNAPSHOT.jar \
  root@121.40.188.98:/opt/app/games/

scp -i "/Users/apple/Downloads/Milvus.pem" \
  chat-media/target/chat-media-0.0.1-SNAPSHOT.jar \
  root@121.40.188.98:/opt/app/media/

# 4. 上传前端静态资源到主服务器 Nginx
scp -r -i "/Users/apple/Downloads/我的密钥.pem" \
  frontend/dist/* \
  root@112.124.106.108:/opt/app/static/chat/
```

### 3.3 重启服务

```bash
# 单独重启（改了哪个重启哪个）
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "bash /opt/app/restart-core.sh"      # 重启 core
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "bash /opt/app/restart-web.sh"       # 重启 web (仅 8081)
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "bash /opt/app/restart-games.sh"     # 重启 games
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "bash /opt/app/restart-media.sh"     # 重启 media

# 全量重启
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "bash /opt/app/restart-all.sh"
```

### 3.4 环境变量

所有 start/restart 脚本都加载 `/opt/app/.env` 文件，包含：
- `DB_USERNAME` / `DB_PASSWORD` — MySQL 连接凭据
- `REDIS_HOST` / `REDIS_PORT` — Redis 连接
- `RABBITMQ_HOST` / `RABBITMQ_PORT` — RabbitMQ 连接
- LLM API Key（千问、DeepSeek、豆包）

### 3.5 意图识别配置

#### 环境变量

| 环境变量 | 默认值 | 说明 |
|---------|-------|------|
| `INTENT_ENABLED` | `true` | 是否启用意图识别 |
| `INTENT_MODEL` | `qwen-turbo` | L3 分类用 LLM 模型 |

> L2 阈值已改为**动态自适应**（无需手动配置），详见下节。

#### 三层漏斗结构

| 层 | 引擎 | 命中率 | 延迟 | 关键特征 |
|----|------|--------|------|---------|
| L1 | `RuleBasedMatcher` | 15-25% | 0-1ms | 33 条规则覆盖 10 类意图 |
| L2 | `ContextMatcher` | 80-90% | 30-80ms | Milvus k-NN + 动态阈值 + 种子预加载 |
| L3 | `ToolIntentMatcher` | <10% | 200-1000ms | LLM 分类兜底 + 反馈闭环 |

#### v3.0 新增优化

**L1 规则层**：
- 规则从 10 条扩展到 33 条，覆盖全部 10 种意图
- 额外支持 `CREATIVE_WRITING`、`KNOWLEDGE_QA`、`TASK_EXECUTION`、`EMOTIONAL_SUPPORT` 等

**L2 语义层**：
- 启动时从 `intent-seeds.json` 自动加载 ~420 条种子示例到 Milvus
- **滑动窗口动态阈值**：每意图最近 1000 次命中率 → 自动 ±0.02 调节阈值
- **增强上下文消歧**：多轮同主题加分 / 代码场景降权 / 情绪连续性加分
- **nprobe 自适应**：集合大小 <1000→32、<5000→16、≥5000→8

**反馈闭环**：
- L3 高置信 (>0.80) 分类结果 → 异步推入 SeedPool
- 积累 100 条 或 每 10 分钟 → 自动写入 Milvus (L2) + 提取关键词 (L1)
- 持续自增强，L1+L2 命中率目标 >95%

**多级 TF-IDF**：
- 自动规则挖掘阈值从 tf>0.2 降为三级 (0.05/0.08/0.15)，关键词发现量提升 5×

#### 运维命令

```bash
# 查看漏斗命中率
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "grep 'IntentFunnel\|RuleMatcher\|ContextMatcher\|SeedPool\|质量' /opt/app/logs/core-9090.log | tail -20"

# 查看 L2 动态阈值和每意图命中率
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "grep '阈值[上下]调\|per_category_hit_rate' /opt/app/logs/core-9090.log | tail -10"

# 查看种子池状态
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "grep 'SeedPool' /opt/app/logs/core-9090.log | tail -10"

# 手动触发种子池刷新 (管理接口)
curl -X POST http://121.40.188.98:9090/internal/admin/intent/seed-flush \
  -H "X-Admin-Password: your-password"

# 重置漏斗指标
curl -X POST http://121.40.188.98:9090/internal/admin/intent/reset-stats \
  -H "X-Admin-Password: your-password"
```

---

## 四、Docker 部署

### 4.1 配置文件

| 文件 | 用途 |
|------|------|
| `Dockerfile-core` | chat-core 镜像（多阶段构建，maven → jre-alpine） |
| `Dockerfile-web` | chat-web 镜像 |
| `Dockerfile-games` | chat-games 镜像 |
| `Dockerfile-media` | chat-media 镜像 |
| `docker-compose.yml` | 完整编排（含 MySQL、Redis、RabbitMQ、Nacos） |

### 4.2 使用方式

```bash
# 仅中间件（本地开发）
docker compose --profile dev up -d

# 全量部署
docker compose --profile all up -d

# 停止
docker compose --profile all down
```

### 4.3 服务端口映射

| 容器 | 端口 |
|------|------|
| chat-core | 9090 |
| chat-web | 8081 |
| chat-games | 8083 |
| chat-media | 8084 |
| frontend (Nginx) | 80 |
| MySQL | 3306 |
| Redis | 6379 |
| RabbitMQ | 5672 (AMQP) / 15672 (管理界面) |
| Nacos | 8848 |

---

## 五、日志与监控

### 5.1 查看日志

```bash
# 前端错误上报日志（主服务器）
ssh -i "/Users/apple/Downloads/我的密钥.pem" root@112.124.106.108 \
  "grep '前端异常' /opt/app/logs/app-8081.log | tail -50"

# 后端各模块日志（Milvus 服务器）
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "tail -100 /opt/app/logs/core-9090.log"     # core

ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "tail -100 /opt/app/logs/web-8081.log"      # web

ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "tail -100 /opt/app/logs/games-8083.log"    # games

ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "tail -100 /opt/app/logs/media-8084.log"    # media
```

### 5.2 查看服务进程

```bash
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "ps aux | grep java | grep -v grep"
```

### 5.3 Nacos 控制台

- 地址：http://121.40.188.98:8848/nacos/
- 账号/密码：`nacos` / `nacos`

### 5.4 监控运维 (开发环境)

Docker Compose 启动监控栈：

```bash
cd /Users/apple/IdeaProjects/chat-system-project
docker compose --profile monitoring up -d
```

| 组件 | 地址 | 说明 |
|------|------|------|
| **Prometheus** | http://localhost:9091 | 指标采集 + 告警 |
| **Grafana** | http://localhost:3000 (admin/admin) | 仪表盘可视化 |
| **Zipkin** | http://localhost:9411 | 分布式链路追踪 |
| **Actuator Prometheus** | http://localhost:8080/actuator/prometheus | 原始指标数据 |
| **Swagger UI** | http://localhost:8080/swagger-ui.html | API 文档 |

### 5.5 熔断与弹性

| 能力 | 实现 | 说明 |
|------|------|------|
| 熔断降级 | Resilience4j CircuitBreaker | 50% 失败率 → 30s 熔断 |
| 自动重试 | Resilience4j Retry | 最多 3 次，间隔 500ms |
| 超时保护 | Resilience4j TimeLimiter | 单次调用 30s 超时 |
| 调用链追踪 | Micrometer + Brave + Zipkin | 跨度采样 10% |

---

## 六、运维脚本索引

所有脚本位于 `scripts/` 目录：

| 脚本 | 说明 |
|------|------|
| `deploy.sh` | 一键 CI/CD 部署 |
| `load-test-500.py` | 500 并发压测（支持 HTTP/WS/AI 模式） |
| `restart-*.sh` | 各模块重启 |
| `start-*.sh` | 本机启动 |
| `stop-background.sh` | 停止后台服务 |
| `daemon-core.sh` | core 守护进程 |
| `nginx-*.sh` | Nginx 配置管理 |
| `db-migrate.sh` | 数据库迁移 |
| `chaos-test.sh` | 混沌工程测试 |
| `security-scan.sh` | 安全扫描 |
| `sre-monitor.sh` | SRE 监控告警 |
| `cost-analysis.sh` | 成本分析 |

### 6.2 思考链展示排障

```bash
# 查看 ParsingStream 相关日志
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "grep 'ThinkingStream\|thinking_start\|isComplexIntent' /opt/app/logs/core-9090.log | tail -20"
```

**常见问题**：

| 现象 | 可能原因 | 排查方式 |
|------|---------|---------|
| 复杂问题无灰色思考链 | LLM 未输出 `<thinking>` 标签 | 查看 LLM 原始输出确认是否包含标签 |
| 思考链内容中断/不完整 | 流式输出标签被 cross-chunk 切断 | `emitSafe()` 缓冲 11 字符防此问题，检查 LLM 原始输出 |
| 简单问题也出现灰色区域 | 意图误判为复杂意图 | 查看 `IntentFunnel` 日志确认最终意图 |
| 前端无渲染 | 前端未处理 `thinking_token` | 检查浏览器 WebSocket 消息，确认消息类型 |

> **Note**：思考链功能内置于 ChatProcessor / TreeHoleService，无需额外配置或重启，部署后自动生效。

---

## 七、压测报告

> 压测脚本: `scripts/load-test-500.py` | 压测时间: 2026-08-11 | 目标: `112.124.106.108`

### 7.1 纯消息写入（ai_answer=false，随机 IP 绕过限流）

| 并发数 | 成功率 | P50 | P90 | P99 | Max | 平均 | 吞吐量 |
|--------|--------|-----|-----|-----|-----|------|--------|
| 100 | 100% | 75ms | 92ms | 262ms | 311ms | 84ms | 16.7 req/s |
| 500 | 100% | 75ms | 92ms | 262ms | 311ms | 84ms | 16.7 req/s |
| 1,000 | 100% | 70ms | 84ms | 248ms | 309ms | 78ms | 33.3 req/s |
| 5,000 | 100% | 71ms | 92ms | 255ms | 331ms | 81ms | 166 req/s |

**结论**：DB 写入 + Redis 计数链路无瓶颈，5000 并发零失败，P99 < 350ms。

### 7.2 AI 回答（ai_answer=true，消息入队 + 异步 LLM）

> 优化配置：HikariCP 5→30, Tomcat threads 50→200, Nginx worker_connections 1024→4096

| 并发数 | 成功率 | P50 | P90 | P99 | Max | 平均 | 备注 |
|--------|--------|-----|-----|-----|-----|------|------|
| 10 | 100% | 83ms | 140ms | 140ms | 140ms | 91ms | -- |
| 50 | 100% | 82ms | 112ms | 193ms | 193ms | 89ms | -- |
| 200 | 100% | 78ms | 117ms | 2.6s | 2.7s | 179ms | -- |
| 500 (优化前) | 99.8% | 4.3s | 8.1s | 9.7s | 11.7s | 4.1s | 1 次 502 |
| **500 (优化后)** | **100%** | **154ms** | **4.4s** | **5.6s** | **6.3s** | **1.6s** | ✅ 零失败 |

**结论**：AI 模式下 HTTP 同步部分（消息入库 + 入队）响应快速；500 并发优化后 P50 从 4.3s 降至 154ms，零 502。

### 7.3 限流验证

| 场景 | 结果 |
|------|------|
| 1000 并发（同一 IP） | 前 495 成功，后 505 全部 429 限流 |
| 自动拉黑 | 超频后 IP 被临时封禁，返回 `"您的IP已被临时封禁"` |
| 随机 IP 模拟 | 1~5000 并发均不受限流影响 |

限流策略：用户级 20次/分钟，IP 级 600次/分钟，自动拉黑 1000次/60秒。

### 7.4 1000 并发优化指南

> 500 并发 AI 模式下出现 502 的根因及修复方案，已验证可支撑 500 并发零失败。

#### 瓶颈分析

| 组件 | 优化前 | 优化后 | 说明 |
|------|--------|--------|------|
| **chat-web HikariCP** | `maximum-pool-size: 5` | `30` | 500 并发竞争 5 个 DB 连接 → 大量等待 |
| **chat-web Tomcat** | `threads.max: 50` | `200` | 500 并发只有 50 线程 → 排队堆积 |
| chat-web Tomcat | `max-connections: 500` | `2000` | 连接上限不够 |
| chat-web Tomcat | `accept-count: 50` | `200` | OS 层等待队列不够 |
| chat-core HikariCP | `maximum-pool-size: 10` | `30` | web 转发到 core 也会消耗连接 |
| chat-core Tomcat | `threads.max: 100` | `200` | 对齐 web 层 |
| **Nginx** | `worker_connections: 1024` | `4096` | 1000 并发时连接数不足 |
| **Nginx API** | 无 `proxy_read_timeout` | `120s` | 默认 60s，长请求被 Nginx 切断 |
| Nginx upstream | 无 `keepalive` | `64` | 每次请求新建 TCP 连接 |

#### 1. chat-web (application-prod.yml)

```yaml
server:
  tomcat:
    threads:
      max: 50 → 200          # 线程池
      min-spare: 10 → 20
    accept-count: 50 → 200   # 等待队列
    max-connections: 500 → 2000  # 最大连接

spring:
  datasource:
    hikari:
      maximum-pool-size: 5 → 30   # 数据库连接池（最关键）
      minimum-idle: 1 → 5
```

#### 2. chat-core (application-prod.yml)

```yaml
server:
  tomcat:
    threads:
      max: 100 → 200
    accept-count: 100 → 200
    max-connections: 1000 → 2000

spring:
  datasource:
    hikari:
      maximum-pool-size: 10 → 30
      minimum-idle: 2 → 5
```

#### 3. Nginx (nginx.conf)

```nginx
events {
    worker_connections 1024 → 4096;    # 允许更多并发连接
}

upstream backend_nodes {
    ip_hash;
    server 172.23.172.13:8081 max_fails=2 fail_timeout=5s;
    keepalive 64;                      # 新增：连接池复用
}

location /api/ {
    proxy_pass http://backend_nodes;
    proxy_connect_timeout 5s;
    proxy_read_timeout 120s;           # 新增：防止长请求超时
    proxy_send_timeout 120s;           # 新增
    proxy_next_upstream error timeout http_502 http_503 http_504;
    proxy_next_upstream_tries 2;
}
```

#### 优化效果

| 指标 | 优化前 (500 并发) | 优化后 (500 并发) |
|------|-------------------|-------------------|
| 成功率 | 99.8% | **100%** |
| P50 | 4318ms | **154ms** |
| 502 错误 | 1 次 | **0** |

### 7.5 压测命令

```bash
# 基础用法
python3 scripts/load-test-500.py --users=500 --rampup=30

# AI 模式（触发真实 LLM 调用）
python3 scripts/load-test-500.py --users=50 --rampup=20 --ai --yes

# 其他参数
# --host     目标地址 (默认: http://112.124.106.108)
# --users    并发用户数 (默认: 500)
# --rampup   爬坡秒数 (默认: 30)
# --timeout  请求超时秒数 (默认: 60)
# --ai       触发 AI 回答
# --light    纯 HTTP 模式，不连 WebSocket
# --yes      跳过 AI 确认提示
# --skip-check 跳过预热检查
```

---
## 八、树状辩论模式

### 8.1 功能概述

树状辩论是标准辩论模式的升级版，通过 LLM 语义拆解将问题分解为 2-3 个独立分析视角，每个视角由豆包（正方）、DeepSeek（反方）、千问（中立）进行 3 轮辩论，最后汇总为一份综合分析结论。

### 8.2 架构流程

树状辩论采用 **LangGraph 混合模式**：顶层分解和汇总由 Java 方法完成，单个视角内的 3 轮辩论由 LangGraph 子图驱动。

```
用户提问
   │
   ▼
┌──────────────────────────────────────┐
│ 1. 语义拆解 (decompose)              │
│    LLM 拆解 → 2-3 个分析视角          │
│    异常回退 → 理性分析/批判观点/综合考量 │
│    (Java 方法)                        │
└──────────┬───────────────────────────┘
           │
     ┌─────┴─────┬─────────┐
     ▼           ▼         ▼
┌─ 视角1 ─┐ ┌─ 视角2 ─┐ ┌─ 视角3 ─┐   ← 上层 batchPool 并行
│         │ │         │ │         │
│  ┌──────┴─┴─ LangGraph 子图 ─┴─┴──────┐
│  │  START → debate → shouldContinue  │
│  │              ↑         ↓          │
│  │              └─── (round < 3)     │
│  │                        (round≥3)→ │
│  │              summary → END        │
│  └───────────────────────────────────┘
│         │ │         │ │         │
│  视角结论  │ │ 视角结论  │ │ 视角结论   │
└──────┬────┘ └──────┬────┘ └──────┬────┘
       └──────────────┼──────────────┘
                      ▼
          ┌───────────────────────┐
          │ 2. 最终汇总 (aggregate)│
          │    各视角结论 → LLM    │
          │    非流式，直接返回    │
          │    异常 → 本地拼接     │
          │    (Java 方法)         │
          └───────────────────────┘
```

**关键模块**：

| 模块 | 文件 | 说明 |
|------|------|------|
| 顶层编排 | `chat-core/.../DebateTreeProcessor.java` | 拆解 + 并行调度视角 + 汇总 |
| LangGraph 子图服务 | `chat-core/.../langgraph4j/TreePerspectiveGraphService.java` | 构建单视角 LangGraph 图并执行 |
| LangGraph 子图节点 | `chat-core/.../langgraph4j/TreePerspectiveNodes.java` | debate / shouldContinue / summary 三节点 |
| LangGraph 状态 | `chat-core/.../langgraph4j/TreePerspectiveState.java` | 轮次历史、各方答案、结论 |
| 前端展示 | `frontend/src/components/DebateTreeView.jsx` | 拖拽画布 + 节点树 + 缩放 |
| 前端页面 | `frontend/src/pages/Debate.jsx` | 树状模式入口 + WS 事件路由 |
| 后端测试 | `chat-core/.../DebateTreeProcessorTest.java` | 顶层单元测试 |
| LangGraph 测试 | `chat-core/.../langgraph4j/TreePerspectiveStateTest.java` | 状态结构验证 |

### 8.3 WebSocket 事件流

| 事件类型 | 方向 | 说明 |
|----------|------|------|
| `start` | 服务端→前端 | 模型信息 (model_id→model_name) |
| `tree_decompose_start/result` | 服务端→前端 | 语义拆解结果 |
| `tree_perspective_start` | 服务端→前端 | 视角开始辩论 |
| `tree_round_start/end` | 服务端→前端 | 每轮开始/结束 |
| `tree_stream_token` | 服务端→前端 | 流式 token 推送 |
| `tree_round_response` | 服务端→前端 | 单角色完整回答 |
| `tree_perspective_concluding` | 服务端→前端 | 视角总结中 |
| `tree_perspective_summary` | 服务端→前端 | 视角结论 |
| `tree_aggregate_start` | 服务端→前端 | 汇总开始 |
| `tree_aggregate_result` | 服务端→前端 | 汇总结果 (一次性推送) |
| `tree_perspective_error` | 服务端→前端 | 视角辩论异常 |
| `done` | 服务端→前端 | 辩论完成 |
| `error` | 服务端→前端 | 全局异常 |

### 8.4 容错设计

| 场景 | 处理策略 |
|------|---------|
| 语义拆解 LLM 失败 | 回退到默认 3 视角 (理性分析/批判观点/综合考量) |
| 语义拆解返回空/>4 个 | 回退到默认 3 视角 |
| 单个视角辩论异常 | 标记 `[xxx 辩论中断]`，其他视角继续 |
| 汇总 LLM 超时/失败 | 本地拼接各视角结论，不触发全局异常 |
| 全部异常 | 仍发送 `done` 消息，前端可解除 loading 状态 |

### 8.5 排障

```bash
# 查看树状辩论相关日志
ssh -i "/Users/apple/Downloads/Milvus.pem" root@121.40.188.98 \
  "grep '\[TreeDebate\]' /opt/app/logs/core-9090.log | tail -50"
```

| 现象 | 可能原因 | 排查 |
|------|---------|------|
| 拆解后无视角 | LLM 返回非法 JSON | 查看 `tree_decompose_result` 中 perspectives 是否为空 |
| 某个视角卡住 | 3 个 LLM 模型中有模型超时 | `grep "error:" /opt/app/logs/core-9090.log` |
| 最终结论不显示 | `done` 消息未到达前端 / 前端组件提前卸载 | 检查 WS 消息中 `done` 是否在 `tree_aggregate_result` 之后 |
| 汇总卡死 | `aggregate()` prompt 过大 + 流式输出 | 已改为 `invoke()` 非流式，若仍卡死检查 LLM API 限制 |
| 移动端缩放/拖拽无效 | CSS `touch-action` 冲突或 JS 事件被拦截 | 确认 `debate-tree-wrapper` 有 `touch-action: none` |

### 8.6 LangGraph 混合模式设计

树状辩论中，**视角内的辩论循环**使用 LangGraph 的 `StateGraph` 做有向无环图编排：

| 设计 | 说明 |
|------|------|
| 图结构 | `debate → shouldContinue → (debate 或 summary → END)` |
| 节点 | 3 个 `AsyncNodeAction`：debateNode、shouldContinue、summaryNode |
| 条件路由 | `shouldContinue` 根据 `round < maxRounds` 决定下一跳 |
| 并发策略 | 视角间在上层 `CompletableFuture` 并行，视角内 LangGraph 单线程串行 |
| 编译缓存 | 每个视角重新编译图（beta API 限制），每次执行约 10-20ms 开销 |

**为什么只做视角内子图而非全图**：

LangGraph4j 的 `StateGraph` 节点是编译时固化的，但树状辩论的视角数量由 LLM 运行时动态决定（2-3 个）。若用单一全图实现，需要预定义 3 条并行分支并用条件跳过未使用的分支，图会变得复杂且不灵活。混合模式 — 上层 Java 动态并行 + 视角内 LangGraph 子图 — 兼顾了灵活性和结构化编排。

### 8.7 与标准辩论的对比

| 特性 | 标准辩论 (DebateProcessor) | 树状辩论 (DebateTreeProcessor) |
|------|---------------------------|-------------------------------|
| 视角数量 | 固定 3 种 (正方/反方/中立) | 动态 2-3 个 (LLM 按问题拆解) |
| 辩论轮次 | 3 轮 | 每个视角 3 轮 |
| LLM 调用 | 每轮 3 次 (流式) | 每视角每轮 3 次 (流式) + 汇总 1 次 (非流式) |
| 总 LLM 调用 | ~9 次 | 视角数 × 9 + 1 次汇总 |
| 前端展示 | 固定布局 | 可拖拽 DAG 画布 + 缩放 |
| 汇总方式 | 逐轮逐步 | 所有视角完成后一次性汇总 |
| 辩论编排 | LangGraph4j `StateGraph` | 上层 Java + 视角内 LangGraph4j 子图 (混合) |
| 异常隔离 | 全局失败 | 单视角失败不影响其他视角 |
